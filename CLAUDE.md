# CLAUDE.md

@AGENTS.md

## 执行职责

你是本项目唯一业务代码执行层。每次执行前必须从磁盘重新读取根目录 `TASK.md`、其指向的当前任务及任务引用的原始需求。只执行当前任务明确授权的范围，不依赖旧会话，不自行重新定义需求，也不决定验收通过。

## 当前项目

当前目录将实现一个面向中国大陆招聘者的“电商运营经验 × AI 产品实践”求职作品集。当前任务确定的技术方案为 Astro、TypeScript、原生 CSS、静态生成和 GitHub Pages。

## Git 身份与项目归属

- 当前机器的全局 Git 作者身份属于实际开发者本人，必须保持不变。
- 网站主体与未来 GitHub 仓库所有者是丰瑞，目标账号为 `rui636290-cpu`。
- Git 提交作者与远程仓库所有者可以不同；不得为了匹配网站主体而改写全局或仓库级 Git 姓名、邮箱。

## 工程原则

- 先审计再修改，保留已有内容和用户变更。
- 使用满足任务的最小实现；优先 Astro、语义 HTML、原生 CSS 和少量原生 JavaScript。
- 不引入 UI 框架、动画库、CMS、后端、数据库或运行时外部资源。
- 所有未知个人信息和结果使用明显占位符，禁止虚构。
- 核心资源本地化；中国大陆访问不依赖 Google Fonts、外部 CDN、远程图片或运行时国外 API。
- 完成后运行当前任务要求的检查与 production build；未运行的检查必须说明原因。

## 权限边界

- 只能修改当前任务“允许修改”列出的文件；固定例外是对应的 `reports/TASK-XXX-REPORT.md`。
- 不修改 `AGENTS.md`、`CLAUDE.md`、根目录 `TASK.md` 或 `tasks/**`。
- 不读取或记录密钥、Token、密码、`.env`、认证文件或 credentials。
- 默认不初始化 Git，不提交、推送、发布或执行破坏性 Git 操作。只有当前任务明确记录用户授权并逐项列出允许的 Git 操作时，才可执行这些明确授权的操作；不得据此扩大到远程、推送或发布。
- 范围或权限不足时停止并报告 `BLOCKED`。

## 任务完成报告（强制）

每次完成、部分完成或阻塞当前任务时，必须在仓库内创建或覆盖对应报告：`reports/TASK-XXX-REPORT.md`，其中 `TASK-XXX` 与当前任务文件名一致。例如 `tasks/TASK-002-UPDATE-PROFILE.md` 的报告必须是 `reports/TASK-002-REPORT.md`。

报告必须包含：

1. 任务文件与执行状态（`COMPLETED` 或 `BLOCKED`）；
2. 实际新增、修改、删除的文件；
3. 实际运行的命令及其结果；
4. 未运行或失败的验证及原因；
5. 范围偏差、已知风险和待补充信息；
6. 明确的一行：`等待用户通知 Codex 审核。`

报告只记录事实，不复制密钥、Token、密码、`.env` 内容或其他凭证。写完报告后停止：不得自行修改根目录 `TASK.md`、创建下一任务、判定 `APPROVED`、推送或发布。默认不得在报告后提交；唯一例外是当前任务明确授权将该报告作为本地审计记录提交，此时只能提交任务指定的报告文件，完成后立即停止等待用户通知 Codex 审核。
