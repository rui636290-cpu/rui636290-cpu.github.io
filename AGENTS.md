# 协作规则

## 角色

- 用户：确定产品目标、优先级、安全敏感授权和最终产品决策。
- Codex：调查、规划、创建任务、治理配置、检查 Diff、运行验证并验收。
- Claude Code：唯一业务代码执行者，只实现根目录 `TASK.md` 指向的当前任务。

Codex 默认不修改业务源代码；可以维护协作规则、任务单、配置草案和验收记录。

## 执行规则

- 没有 `TASK.md` 或入口没有指向明确任务时，Claude Code 不修改业务代码。
- 每次执行前从磁盘重新读取 `TASK.md`、其指向的任务文件及任务引用的需求，不依赖旧会话。
- Claude Code 只能修改任务中“允许修改”列出的文件；唯一的固定例外是任务对应的 `reports/TASK-XXX-REPORT.md` 执行报告。
- 不新增需求、不替换技术栈、不顺手重构、不新增非必要依赖。
- 关键歧义、范围外修改或需要账号授权时停止并报告 `BLOCKED`。
- 完成或阻塞时，Claude Code 必须在磁盘写入任务对应的执行报告，然后停止等待用户通知 Codex 审核。
- Git Diff 与实际命令输出是验收依据；口头声称不算通过。

## 任务治理

- 根目录 `TASK.md` 只作为当前任务入口。
- 复杂任务使用 `tasks/TASK-XXX-<TYPE>.md`。
- Claude 开始执行后任务范围和授权冻结；需求变化由 Codex 创建新的 `FIX` 任务。
- 提交、推送、部署和公开发布使用独立 `RELEASE` 任务，并要求用户明确授权。
- 不为读取、搜索或单项检查等微步骤创建任务。

## 流程

1. Codex 调查并创建编号任务，更新根目录 `TASK.md`。
2. Claude Code 从磁盘重新读取并执行当前任务。
3. Claude Code 写入 `reports/TASK-XXX-REPORT.md` 后停止；用户通知 Codex 审核。
4. Codex 检查任务、执行报告、工作区变化和验证输出。
5. Codex 只给出 `APPROVED`、`CHANGES_REQUESTED` 或 `BLOCKED`。
6. 返工由 Codex 创建新的 `FIX` 任务；提交或发布需独立授权。

未经用户明确授权，不初始化 Git，不提交、推送、合并、变基、打标签、部署或发布。
