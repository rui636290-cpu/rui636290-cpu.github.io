# TASK-001-IMPLEMENT：实现面向中国大陆招聘者的个人简历与项目作品集网站

状态：`SUPERSEDED_BY_TASK-002`

## 执行者

Claude Code 是本任务唯一业务代码执行层。每次开始或继续执行时，必须从磁盘重新读取本文件和下方原始需求，不依赖会话中的旧版本。

## 原始需求（必须完整读取）

`D:\Codex\attachments\bc0e179b-ac62-4e74-8857-b14dd37eeb50\pasted-text.txt`

原始需求是本任务的产品与验收基线；本文件只补充执行边界、已知事实和优先级，不替代原文。

## 目标

在 `D:\fr个人网站\fr个人网站` 中直接完成第一版求职型个人网站，让杭州中小企业招聘者在 30 秒内理解候选人的“电商运营经验 × AI 产品实践”、目标岗位、代表项目、本人职责、简历下载与联系方式。

## 已知审计事实

- 当前工作目录：`D:\fr个人网站\fr个人网站`
- 当前目录为空。
- 当前目录不是 Git 仓库，没有分支、历史或已有工作区变更。
- 当前没有 `package.json`、Astro 配置、`src`、`public`、现有页面或部署配置。
- Node.js：`v24.16.0`
- npm：`11.13.0`

开始时仍须自行从磁盘复核以上事实，并先输出简短审计结论、风险和预计新增/修改文件，然后继续实现；除非发现会误改其他仓库的严重风险，否则不要等待确认。

## 实现决策

- 空工作区采用 Astro + TypeScript + 原生 CSS，静态生成。
- 只安装 Astro 及完成构建/检查确实需要的最少依赖；不引入 UI 框架、动画库、图标库、CMS、后端或数据库。
- 优先语义 HTML、CSS 和极少量原生 JavaScript。
- 内容集中在少量易编辑的数据文件中；不要为未来需求设计抽象层。
- 未知个人信息、经历、学校、公司、时间、数据、链接、头像、二维码和 PDF 必须使用原始需求规定的明显占位符，严禁虚构。
- 没有真实图片时使用本地原创抽象 SVG/几何占位视觉；不要生成虚假人物照片。
- 所有核心资源本地化，不使用 Google Fonts、外部 CDN、远程图片、运行时国外 API、统计服务或视频。
- 配置 GitHub Actions Pages 工作流，但不得初始化 Git、提交、推送或发布。
- 因未知 GitHub 用户名和仓库名，GitHub Pages 的 `site`/`base` 应采用可维护且可验证的构建时配置或明确占位配置，并在 README 准确说明用户主页仓库与普通项目仓库的设置差异；不要编造地址。

## 范围与优先级

完整遵循原始需求。发生取舍时按以下顺序：

1. 内容真实性、仓库安全、无境外运行时依赖；
2. 首页 30 秒求职叙事与明显的项目/简历/联系入口；
3. 首页、项目列表、3 个项目详情、在线简历/关于页面；
4. 360px 起的响应式、键盘可用、焦点状态、语义结构、减少动画；
5. 深浅主题（跟随系统、手动切换并持久化）；
6. SEO、静态资源路径和 GitHub Pages 构建兼容；
7. README 的内容替换与部署说明；
8. 仅在不增加明显复杂度时加入克制动效。

## 必须交付

- 可运行的 Astro 静态站点。
- 首页：导航、Hero、3 组核心能力、3 个精选项目、工作经历、关于、求职状态/联系、页脚。
- 项目列表页与 3 个可访问的项目详情页，内容按原始需求的案例结构组织。
- 在线简历/关于页。
- 集中的个人资料、经历和项目数据；占位项易搜索、易替换。
- 本地项目封面/抽象视觉、favicon，以及 `public/resume` 等明确资源位置。
- 响应式导航、深浅主题、清晰焦点、`prefers-reduced-motion`。
- 唯一 title/description、`zh-CN`、canonical、Open Graph、robots、sitemap（使用 Astro 原生能力或最小方案）。
- `.github/workflows/deploy.yml`。
- 完整 README，覆盖原始需求列出的 16 项。

## 禁止项

- 不修改工作区外文件。
- 不读取、要求或记录任何密钥、Token、密码、`.env` 或 credentials。
- 不执行 `git init`、提交、推送、发布、强制重置、变基或删除历史。
- 不虚构候选人事实或量化结果。
- 不照搬参考网站的代码、文案、图片、品牌或素材。
- 不为视觉效果引入重型依赖或大量 JavaScript。
- 不静默改变本 TASK 的范围；如范围或权限需要变化，停止并报告。

## 允许修改

Claude Code 只能在当前工作区内新增或修改以下业务实现范围：

- `package.json`、`package-lock.json`
- `astro.config.*`、`tsconfig.json`
- `.gitignore`
- `.github/workflows/deploy.yml`
- `src/**`
- `public/**`
- `scripts/**`
- `README.md`

不得修改 `AGENTS.md`、`CLAUDE.md`、根目录 `TASK.md`、`tasks/**` 或其他未列出的文件。若实现确需扩大范围，停止并报告 `BLOCKED`，由 Codex 创建新任务或新阶段。

## 验证

使用项目实际脚本完成并记录：

- 依赖安装成功；
- Astro/TypeScript 检查；
- production build；
- 如项目提供 lint/test，则运行；
- 检查生成页面、内部链接、静态资源路径与 GitHub Pages base path；
- 检查 360、390、640、768、1024、1440px 关键布局，特别避免 Hero 固定 `min-height` 造成移动端大块空白或横向滚动；
- 检查菜单的键盘操作、Escape 关闭、主题持久化和减少动画；
- 检查构建产物中无核心外部 CDN/Google Fonts/远程图片/运行时 API；
- 检查首页仅 3 个精选项目、非首屏图片懒加载、图片尺寸声明和合理体积；
- 尽可能使用本地浏览器做桌面端与移动端视觉检查，并修复明显问题。

若某项因环境限制无法验证，明确写出未运行原因，不得声称通过。

## 完成报告

结束时按以下结构报告：

1. 审计结论；
2. 实际修改、新增、删除文件；
3. 页面结构与已完成功能；
4. 实际运行的命令及结果；
5. 未完成或未验证事项；
6. 用户必须替换的信息；
7. 本地预览命令；
8. GitHub Pages 部署步骤及两类仓库差异；
9. 中国大陆访问风险；
10. 下一步建议。

不要执行提交、推送或发布。
